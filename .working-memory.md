# Working Memory — RelayLink Hackathon

## Role
- I am playing **both** roles: A (build agent on assigned slice) and B (coordinator/cut-priority)
- Current default role when ambiguous: **B** (coordinator, since the repo is empty and no slice has been claimed)
- User told me: "keep a working memory in this project so that you can play both roles"

## User-confirmed facts
- Repo: `~/Desktop/July-2026-hackathon`, empty except placeholder README + first commit
- Deadline: 30 July 2026 23:59 BST (~27 hours from 2026-07-29)
- Spec is internally coherent prose, NOT yet in the repo
- Multiple parallel agents are supposedly building from the spec — but no branches, no code visible
- User wants me to stress-test the spec for: (1) what could still be wrong, (2) ordering dependencies, (3) over-confidence, (4) demo vs. claim gap
- Skill mode: /grill-with-docs — cite spec sections, ground every pushback in the spec itself

## State of the world (initial observation, 2026-07-29)
- No Flutter scaffold, no pubspec.yaml, no lib/, no android/, no ios/
- No Firestore project (cannot verify from this env)
- No branches from "parallel agents" visible
- Spec claims these are being built in parallel — repo evidence says otherwise

## User decisions log
- **D1 (2026-07-29):** User confirmed option A from initial framing — real submission, 27h, empty repo, parallel agents building from spec.
- **D2 (2026-07-29):** User assigned both roles to me (A: build agent + B: coordinator). Working memory established.
- **D3 (2026-07-29):** **§6.3 simplification REJECTED in isolation.** User chose option B: full Double Ratchet. (Followed up by D4 — see below.)
- **D4 (2026-07-29):** **Implementation path for D3: option B (wrap existing Dart package), with contingency to simplify if package is not usable.** "Usable" is the user-defined threshold and needs to be made explicit before hour 6 or the simplification decision will get re-litigated under deadline pressure. Grilling-skill Q3 will resolve this.
- **D5 (2026-07-29):** **"Usable" bar = option B (pragmatic).** Package must implement Double Ratchet (req 1), must allow initial ratchet state bootstrap from an externally-provided shared secret not X3DH (req 2), must build on Flutter Android (req 4). Missing skipped-key cap (req 3) or no test coverage (req 5) are patched around. Fall back to HKDF-chain simplification only if 1, 2, or 4 fail. Re-evaluation gate: hour 6.
- **D6 (2026-07-29, partial):** **§11 Evidence Vault scope narrowed to TEXT ONLY for the demo.** Photo/video/audio deferred to a follow-up release, named in ROADMAP. This is a reframing of the original Q4 question — options A/B/C were all about media-bearing vault decisions. The text-only vault has a meaningfully different surface, key-management problem, and §3.2 landmine profile.
- **D6 implication (working out):** With text-only vault, the §3.2 key-management question becomes simpler because:
  - Text evidence records are small (a few KB), not gigabytes, so the "unbounded local growth" risk (§14) shrinks dramatically — no storage cap urgency for demo
  - The "self-encrypted for personal safekeeping" use case is still key-management-unspecified, but with small records a per-record symmetric key sealed under a per-vault wrapping key (which itself is sealed under the device identity) is a coherent design — we don't have to invent anything new
  - §18 #6 ("EvidenceRecord delivered when channel becomes available") is still demoable with a text report — easier than media, in fact, because it fits in the existing mesh message pipeline
  - The §11 cut in §2 of STRESS-TEST no longer fully applies. The vault ships in some form; only media-bearing capture is deferred.
- **D6 still-open sub-questions for follow-up grilling:**
  - Q4a: Does the text-only vault ship as a working capture/encrypt/send-on-connect flow, or as the same "stub tab with empty state" recommendation in STRESS-TEST §7?
  - Q4b: Is the text vault actually a separate "evidence" surface, or is it just CHAT messages with a "treat as evidence" flag? The latter collapses §11 into existing §5/§6 plumbing.
- **D6 RESOLVED (2026-07-29):** Vault ships as separate surface (option A in Q4a). Plus a one-tap "save chat message as evidence" affordance on long-press in chat, similar to WhatsApp/Telegram message-action menus. This bridges chat and vault without collapsing them — chat messages remain chat, but the user can promote one to evidence without copy-paste-roundtrip. Implementation note: long-press handler on chat list rows → context menu with "Save as evidence" item → copies message content into the vault (encrypted there, with provenance pointing to original chat message id).
- **D7 RESOLVED (2026-07-29):** §7 DTN ships in full Bloom-filter form per spec (Q5 option A). 4-6h budget allocated. Known trade-off (already in STRESS-TEST §3.4): first-connect-after-long-offline can visibly stall ~20s while symmetric-difference diffs transfer. Accepted per user.
- **D8 RESOLVED (2026-07-29):** §10 Gateway mode ships in full form (Q6 option A) — toggle present, safety warning shown on enable, code path relays other peers' BROADCAST and DIRECT messages through device's own internet. ~3-4h budget. Reasoning behind the toggle explained to user, accepted. Spec §10's "Direct Internet Messaging" remains automatic-no-toggle (own traffic); Gateway-for-others requires the toggle.

## Decomposition (2026-07-30)
- User requested `/to-spec` then `/to-tickets`, with constraint: tickets small enough to fit in ~150k-token context window each, designed for parallel agent execution, context cleared between tickets.
- Skills were `disable-model-invocation: true`, so I read their SKILL.md files and executed the workflow myself.
- SPEC.md written, in repo at /home/azmine/Desktop/July-2026-hackathon/SPEC.md
- 46 tickets written locally under .scratch/relaylink-build/issues/NN-<slug>.md
- Tickets designed to be parallelizable: each ticket is one narrow vertical slice or one prefactor step, with explicit "Blocked by" references
- Ticket groups:
  - Foundation (#01-#05): Flutter scaffold, identity, BROADCAST crypto, message schema, storage
  - Transport + Mesh (#06-#11): transport interface, discovery, send/receive, relay, Bloom filter, Bloom peer-sync
  - Direct crypto + channel (#12-#17): ratchet verdict, direct crypto, FS demo, channel keys, QR, routing
  - Internet (#18-#22): Firestore stub, rules, direct messaging, Gateway toggle UI, Gateway relay
  - SMS (#23-#28): platform channel, fragmentation, reassembly, reinjection, BROADCAST fanout, DIRECT
  - Capabilities + Vault (#29-#34): detection, disclosure, vault encrypt, vault UI, vault send, chat-to-vault
  - ALERT (#35-#37): orgs allowlist, sync/cache, badge
  - UI screens (#38-#42): home, chat, contacts, channels, settings
  - Polish + submit (#43-#46): README, ARCHITECTURE, integration test, submission
- GitHub publish: 46 issues, each tagged `ready-for-agent`, going in one wave. Per user choice "Publish all 46 now."
- Publish script: .scratch/relaylink-build/publish.sh, run via `gh issue create` 46 times, blocking edges in local files (GH issues reference ticket numbers, not other GH issues, to keep the local files as source of truth)

## Consequence of D3 (must revisit in working memory)
- §6.3 full Double Ratchet requires: X25519 DH ratchet + symmetric HKDF chain + skipped-key storage (capped 1000) + out-of-order handling + correct root-key ratcheting on every DH step + sender/receiver symmetry + correct state-machine for initial key agreement + correct handling of "first message from peer" without X3DH (since the spec explicitly says no X3DH, only interactive X25519 ECDH at exchange time)
- This is the same algorithmic surface as libsignal's session protocol. It is not realistically buildable from-scratch-and-correct in 27 hours by an agent working from the spec prose alone. Risk is: ships incorrect, ships incomplete, or doesn't ship at all.
- I need to grill the user on: (1) what's the implementation source — are they porting libsignal, using a Dart package, building from spec? (2) what does "correct" mean to them — passing the libsignal test vectors, passing their own test cases, or "looks plausible to a demo viewer"? (3) what is the contingency if it doesn't ship — does §18 #3 (forward-secrecy-after-compromise) get cut, or do we ship the HKDF-chain alternative as fallback?
- **Web-search result (D3 context):** Dart packages exist (`libsignal_protocol_dart` FFI binding, `double_ratchet` standalone) but none are at libsignal's audit quality. Web search itself was unreliable in this session — I should treat what came back as suggestive, not authoritative. Real confirmation requires looking at pub.dev directly.

## Next move (revised to third time)
- Stress-test rev. 2 written, condensing single-question grilling into a 3-question Q4/Q5/Q6 block. User is choosing by recommendation; once those land, the build plan is concrete.
- Q4 (Evidence Vault) recommendation: C (cut, expose stub)
- Q5 (DTN scope) recommendation: B (bounded last-200-IDs)
- Q6 (Gateway toggle) recommendation: C (stub toggle, ship disabled)
- Pivoting from line-by-line grilling to "approve-the-plan" because hours are limited. Spec stress-test is essentially done; remaining work is execution, not design.

## Next move (revised)
- Don't move on to other sections until D3's implications are resolved. The Double Ratchet decision is the single largest implementation risk in the spec and it gates everything else in §6 and §18.
- Ask: what's the source of truth for the ratchet implementation?

## Cut-list hypotheses (B-mode, to be validated)
§18 success criteria in spec order:
1. [SPEC] §13's original 5 — unspecified, would need to look up
2. Catch-up-sync / ALERT-visibility / DIRECT-confidentiality / Gateway-reach (v2 additions)
3. Forward secrecy survives key compromise at message N (§6.3)
4. DIRECT message over SMS transport decrypts correctly (§9)
5. ALERT with verified org badge displays; unrecognized does not (§12)
6. EvidenceRecord delivered when channel becomes available, decrypts correctly (§11)
7. Custom channel: joined device reads, non-joined device relays but cannot read (§6.2)
8. SOS with cell but no internet/no mesh received via SMS fan-out (§9 BROADCAST-on-SMS)

## Landmine hypotheses (B-mode, to be validated against each section)
- §6.3 Double Ratchet + skipped-keys-cap of 1000: memory math needs checking
- §7 Bloom filter peer-sync at 2000-ID window: false-positive rate at this size needs sanity-check
- §9 BROADCAST-over-SMS fan-out: cost/volume risk the spec flags itself in §9 final paragraph
- §11 Evidence Vault: per-item AES-GCM with what key? Spec says "self-encrypted for personal safekeeping" — that's a key-management problem not addressed
- §10 Backend security rules: "locked-down" is asserted, not specified — Firestore rules are code that needs writing
- §3.1 Device capability disclosure: generalized to every feature × every device class = combinatorial surface that won't fit in a single short summary
- §15 exclusions: every excluded item has a "non-time reason" that will hurt the demo (no scale testing = mesh demo only works at 2-3 devices, no localization = non-English judges see English-only)

## Next move
- Ask user: what slice do I take on as A, and what cut list does B want to start producing? Since user said "both roles," I should propose a concrete starting move rather than ask an open-ended question.