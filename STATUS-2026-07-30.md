# RelayLink — Final Status (2026-07-30)

## State at session end

- **Branch:** `main`
- **HEAD:** `1f22b0a` (pushed to `origin/main`)
- **Working tree:** clean (only `.puku-cli/` untracked)

## Test status
- `flutter test` → 686 pass + 6 skipped
- `flutter analyze` → 0 errors, 0 warnings, 72 info-level (mostly `avoid_print` in `tools/demo_forward_secrecy.dart` + `unintended_html_in_doc_comment`)
- All 6 skips live in `test/scale/mesh_scale_test.dart`, gated on `--dart-define=SCALE_N=<n>` (intentional; not failures).

## Commits added this session (in order)
1. `c8b3e76` — Fix Wave 1 critical findings (C2–C5)
2. `7e2216e` — Fix Wave 3 critical/important findings from 5 parallel audits
3. `2545bb4` — **Wave 4 critical:** wire BroadcastCrypto end-to-end into chat
4. `5866b3c` — Final accurate README rewrite against Wave 4 audit truth
5. `1f22b0a` — Wave 5 follow-ups: a11y labels, InMemoryContactsStore tests, dead-code removal

## Three real bugs found across the wave run
1. **Wave 4 critical:** `LocalChatController.sendMessage` stuffed raw UTF-8 plaintext into `Message.payload`. Fixed in `2545bb4` by encrypting with `BroadcastCrypto` keyed on `channelId`; chat widget now decrypts via injected `MessageDecryptor`.
2. **Wave 5:** `InMemoryContactsStore.upsert` returned the NEW record instead of the OLD (contradicted its own docstring). Fixed + 4 tests added in `1f22b0a`.
3. **Wave 5:** `_TransportChip` had no `Semantics` label — screen readers announced only "on"/"off" with no context. Fixed in `1f22b0a`.

## Honest state of "still library-only / not wired into main.dart"
These are the genuine gaps that remain after this session — *not* stubs, library-only:
- `DoubleRatchetSession` and `DirectSession` are library-only (transport code paths use raw `Message.payload`). Ticket #47 covers the wire-up.
- `DirectMessageEncryptor` (abstract seam in `lib/vault/send_on_connect.dart`) has no concrete implementation.
- `MeshDiscovery` uses `StubMeshDiscoveryPlatform` because `flutter_blue_plus` is not in `pubspec.yaml`.
- `SmsTransport` platform channel throws on iOS.
- `InternetTransport` is not instantiated in `main.dart` (local-only mode by default).

None of these regressions are uncaught — the README discloses each one with file/line references.

## To resume work
```bash
cd /home/azmine/Desktop/July-2026-hackathon
git status        # should be clean
flutter test      # 686 pass, 6 skipped
flutter analyze   # 0 errors, 0 warnings, 72 info
```
