# 35 — Verified orgs allowlist (Firestore collection + hardcoded entries)

**What to build:** A `verified_orgs/{org_id}` Firestore collection with documents like `{ name: "Demo Red Crescent Branch", public_key: "<base64 Ed25519 pubkey>", added_at: <timestamp> }`. Seed with 2-3 hardcoded entries via Firebase Admin SDK script (committed to repo as `tools/seed_orgs.dart` or similar). Also expose the Ed25519 public keys locally so the receiver can verify signatures offline.

**Blocked by:** #18, #02

**Status:** ready-for-agent

- [ ] `tools/seed_orgs.dart` (or shell-equivalent) creates 2-3 demo entries in Firestore
- [ ] Each entry's Ed25519 public key matches a key generated by `lib/crypto/identity.dart` for that org's identity (test identity created in the seeding script)
- [ ] Org names are clearly demo-flagged in the document data
- [ ] README discloses: "manually curated demo allowlist, not a production trust authority"